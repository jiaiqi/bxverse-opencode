# bxverse Handoff · 2026-09-06（会话交接）

> 定位：**给后续开发 agent 的会话交接**，回答「上一个 agent 做了什么、当前代码是什么状态、接下来做什么、有哪些坑」。
> 正式排期与总纲仍以 `docs/next-development-plan.md`（Phase 4）为准，本文件是其执行现场快照与补遗。
> 日期：2026-09-06。交接时若本文件信息与代码/文档冲突，以代码 + requirements.md + shared/types.ts 为准。

---

## 1. 版本与分支状态

- 主分支 `master`，HEAD `858168c`，**已推 GitHub**（与 origin/master 一致，工作树干净）。
- tag：`v1.2.1` 已重定位到 `790d406`（release 提交）并 force-push；`v1.2.0` 仍指向 `97a9426`。
- 开发分支 `feat/ui-glass-multi` 已合入 master（fast-forward）并保留推送；**新任务直接基于 master 开新分支，勿再在旧分支堆叠**。
- 本地数据目录 `~/.bxverse` 真实使用中（见 §4），**不要用测试/临时 BX_HOME 覆盖它**；core/server 单测各自有 setup.ts 隔离临时 BX_HOME，勿动。
- 6 个 package 版本均为 1.2.1。`pnpm release` 已消费 changeset（`.changeset/feat-ui-glass-multi.md` 已删除）。

## 2. 本会话完成内容（Phase 4 第一批）

### 2.1 R34 玻璃光影视觉体系（UI 3.0 · stone 主题）—— 已合入
- **契约**：`AppConfig.themeStyle` 扩 `'stone'`（`// 扩展：R34`）；server config 保存白名单同步扩。
- **tokens.css**（`apps/web/src/styles/tokens.css`）新增：
  - `html.theme-stone` / `html.theme-stone.dark` 石灰金亮/暗两套 `--bx-*` 全量 token；
  - 玻璃配方：`html.theme-stone .glass-panel/.n-card`（半透 + backdrop-filter + 内高光）；弹层玻璃 `n-modal/n-popover/n-base-select-menu/n-dropdown-menu/n-drawer` + 自绘浮层 `.bx-popover-in`；
  - 环境光层 `body::before`（三团 radial 光晕 + 28s 漂移 `bx-amb`）与细颗粒 `body::after`（SVG feTurbulence）；stone 下 `.bg-bg` 转透明让光场透出；
  - `.bx-hero-tile` 全主题通用暗色玻璃瓦（金色环境光 + hover 光泽扫过）；
  - 降级：`prefers-reduced-transparency` 回实底、`prefers-reduced-motion` 停漂移（媒体查询在 tokens.css 末尾）。
- **接线**：`stores/app.ts`（toggle `theme-stone` class，亮/暗跟随 theme 语义，wenxi 仍强制深色）、`App.vue`（naive overrides 三路分发）、`theme.ts`（stoneLight/DarkThemeOverrides，金主色 亮 #A16207 / 暗 #D9A521，naive 主按钮文字色需覆写保证对比）、`views/Settings.vue`（主题三选一卡片 + grid 改 3 列）。
- **布局对齐 v5**：`AppLayout.vue` 重构为「悬浮玻璃侧栏（232px：BX 品牌标 + 项目快捷列表 + 带文字标签导航 + 底部新手引导）+ 右列（玻璃圆角顶栏 + 主视口）」；旧 w-16 纯图标轨已删。侧栏/顶栏/主视口间用 gap+p-3 悬浮（indigo/wenxi 同骨架自动生效）。
- **Dashboard Bento 化**：hero 瓦（bx-hero-tile，`overview.changedRepos` 按项目聚合 + 直通发版向导）+ 4 指标瓦（移除「版本与备份审计」占位）+ 近 8 周节奏/分支巡检宽瓦。
- **ReleaseWizard 步骤条 v5 化**（用户拍板「保六步，视觉对齐」，**勿重构为单页简报**）：✓ 完成态 + 连接线 scaleX 填充动画 + 当前步品牌光环；a11y role/tab 属性原样保留。
- **ProjectDetail masthead**：大号等宽版本读数右置 + `sharedRepoCount`（同 path 归并的跨项目共享仓库数，品牌 chip）；**RepoDetail 头部**：所属项目 chips「同时在 …」（RouterLink 跳各项目）。

### 2.2 R33 项目与仓库多对多（阶段 1+2 全落地）—— 已合入
- **阶段 1**（前端聚合）：`views/ProjectsManage.vue`（`/projects` 项目卡片网格 + 新建 + 管理仓库对话框）+ `views/RepoRegistry.vue`（`/registry` 按 path 归并 + 所属项目 chips + 接入位统计）；路由与侧栏「管理/注册」入口。
- **阶段 2**（契约，权威源）：
  - shared：`AppConfig.repoRegistry?: RepoDef[]` + `ProjectDef.repoRefs?: string[]`（`// 扩展：R33`，只增）；
  - core `store.ts`：`loadAppConfig` 末尾 `expandProjectRepos`——repoRefs 命中 registry 时展开物化 `repos`（命中优先/未命中丢弃/resolved 空回退内嵌），**不写回**（视图层）；引擎发布语义零变化；
  - server `api/registry.ts`：`GET/PUT /api/repos-registry`（校验 id/name/path + id 去重）、`POST /projects/:id/repos/attach|detach`（body 接受 `repoId` 或 `path`；**首次挂载自动把项目内嵌仓库按 path 归并迁入 registry 并写入 repoRefs**；项目发布中 409 保护）；app.ts 注册；
  - web `api/index.ts`：`repoRegistry/saveRepoRegistry/attachRepo/detachRepo`；ProjectsManage 管理对话框统一走 attach/detach。
- **坑记录**：管理对话框勾选曾用 `NCheckbox`，其受控联动在 `<label>` 内**失效**（点击不触发）——改为原生 `<input type=checkbox>` + `@change`（`accent-color` 用 token），已在代码注释标注。`findRepoDef`/`addRepoByPath` 旧路径已清理。
- **测试**：core `test/store-registry.test.ts`（4 条：无 refs 保留/按序展开/未命中丢弃回退/落盘往返 registry+refs 不丢）；server `test/registry.test.ts`（6 条：初始空/PUT 校验与去重/按 path attach 归并共享/幂等/detach 保留 registry/边界 404·400）。core 188 / server 122 全过。

### 2.3 真实项目接入 —— 已接入「主产品线」p_38zk5w（演示项目 p_vs7tcr 仍在，可删）
| 仓库 | repoId | 分支 | 状态（实测 2026-09-06） |
|---|---|---|---|
| bx-today | r_ikc8y8 | main | 领先 origin 10 提交；**工作区 8 未提交文件**（发布前需先 commit） |
| l-mp-weixin-reface | r_q34vsl | master | 领先 500 提交 |
| l-pc-front | r_t2zytg | master | 领先 500 提交；工作区干净 |
| saas | r_ffa6ak | master | 领先 292 提交；工作区干净 |

- 均「版本未生成」（首次发布推演）。bx-today/l-pc-front/saas 走 packageJson 源，小程序仓走派生。

## 3. 验证基线（交接前全绿）

- `pnpm typecheck` 5/5 · `pnpm test` core 188 / server 122 · `pnpm build` 全链 · `pnpm test:e2e` 三场景（resume/wizard/onboarding）PASSED（向导步骤条改版后重跑确认）。
- 浏览器自查已覆盖：stone 亮/暗、indigo/wenxi 回归、项目管理/注册表/挂载流程、向导步骤条、真实项目详情（stone 主题为当前配置，theme=system）。
- Playwright 复用：临时截图脚本散跑（$TMPDIR/bx…png），无固定回归脚本；改动 UI 后照例开 `http://127.0.0.1:8899` 实测。

## 4. 下一步候选（按价值排序，摘自 next-development-plan Phase 4 剩余 + 本会话观察）

1. **真实发布演练**：在 p_38zk5w 走一次真实发布（建议先让用户在 bx-today commit 掉 8 个未提交文件）。验证六步全流程 + 备份 + 双轨日志落档 + R24/R32 在真实仓的表现。**这是用户最关心的验收**。
2. **glass 材质在 wenxi/indigo 的一致性走查**（stone 已深做，另两主题下 `.glass-panel`/hero 瓦/弹层可能观感不一致）。
3. **Dashboard 增「发布中心」视图**（对齐 v5 的待发布简报聚焦版）与 CommandPalette 命令补全（项目管理/注册表跳转）。
4. `scripts/doctor.mjs` 对 p_38zk5w 跑一次，确认多对多下 doctor 输出正常（expandProjectRepos 对只读诊断的影响）。
5. seed 脚本产物「演示项目」可清（真实项目已是主场景）。

## 5. 关键约定与坑（新增/强化）

- **契约只增不改**：repoRegistry/repoRefs/stone 均标 `// 扩展：`；勿删改旧字段。
- **server 校验白名单**记得同步扩展（config themeStyle、registry 条目、attach body），否则前端通过但保存 400（本会话已踩 config.ts 拒绝 stone）。
- **改 server 必须重启验证**；`node apps/server/dist/index.js`（8899）目前是裸进程，重启命令见 shell 历史 `lsof -ti :8899 | xargs kill`。
- **共享路径跨项目 = 同一仓库**：registry/attach/detach 全部按 `path` 归并（`normPath` 反斜杠→正斜杠）。业务仓不自动 commit——bx-today 的脏文件发布前需人工处理。
- 改 UI 后必开浏览器实测（会话惯例：截图到 $TMPDIR）；e2e 脚本对发布向导步骤结构敏感，改步骤条/向导壳后必跑 `pnpm test:e2e`。
- README「当前状态」仍写 v1.2.1/300 测试；R33/R34 落地后测试数为 188+122+？，**下次文档同步时更新**（本轮未改 README，留待交接者补）。

## 6. 形态基准与相关文件

- 形态基准：`design/bxverse-final-vision-v5.html`（玻璃黑金终局原型）；配套 `design/style-prompt-v5.md`（v5 风格提示词，可喂任意 AI）。
- 产品实现蓝图：`docs/next-development-plan.md` Phase 4；视觉规范 `docs/frontend.md` §15/§16；多对多契约 `docs/data-model.md` §16；需求依据 requirements.md R33/R34。
